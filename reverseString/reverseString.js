var reverseString = function() {
	// change string to an array with each character as element
	// use spread operator to do this: var arr1 = [...str]   (could also use split but that has prolems with unicode)
	// from https://stackoverflow.com/questions/4547609/how-do-you-get-a-string-to-a-character-array-in-javascript#
	
	// reverse the array

	// create new string by joining - join(``) 
}

module.exports = reverseString